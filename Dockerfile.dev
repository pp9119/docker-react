FROM node:alpine

WORKDIR '/app'

COPY --chown=node:node package.json .
#RUN npm install --unsafe-perm=true --allow-root

RUN npm install 

COPY --chown=node:node . .
USER node 
CMD ["npm","run", "start"]
